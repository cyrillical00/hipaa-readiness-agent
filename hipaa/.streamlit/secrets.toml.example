# Anthropic API (existing)
ANTHROPIC_API_KEY = "sk-ant-..."

# Phase 6: local auth allow list. Only used when running outside Streamlit Cloud.
local_allowed_emails = [
    "cyrillical@gmail.com",
    "demo@hipaa.example",
]

# Phase 6: GitHub backed state storage (optional). If unset, falls back to local disk.
HIPAA_STATE_REPO = ""   # e.g. "cyrillical00/hipaa-state"
HIPAA_STATE_PAT = ""    # GitHub PAT with repo scope on the state repo
